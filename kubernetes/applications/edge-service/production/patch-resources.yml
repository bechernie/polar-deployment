apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge-service
spec:
  template:
    spec:
      containers:
        - name: edge-service
          resources:
            requests:
              memory: 256Mi
              cpu: "0.1"
            limits:
              memory: 256Mi
              cpu: "2"
